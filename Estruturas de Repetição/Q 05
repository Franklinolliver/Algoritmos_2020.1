#include <stdio.h>

int main(void) {
  int i,n,soma;
  soma = 0;
  printf("Digite a quantidade de números para somar: ");
  scanf("%d", &n);

  for (i=1; i<= n; i++){
    soma += i;
  }
  printf("%d\n", soma);
  
  return 0;
}
