#include <stdio.h>
#include <string.h>

int main(void) {
  char palavra[100];
  int t,i;

  printf("Digite uma palavra: ");
  scanf("%s",palavra);
  t = strlen(palavra);

  for(i = t-1; i >= 0; i--)
  printf("%c", palavra[i]);
  printf("\n\n");

  return 0;
}
